{% for entity in entities %}
    <tr>
        <td><label class="checkbox no-label" for="checkbox-table-2"><span class="icons"><span class="first-icon fui-checkbox-unchecked"></span><span class="second-icon fui-checkbox-checked"></span></span>
        <span class="icons"><span class="first-icon fui-checkbox-unchecked"></span><span class="second-icon fui-checkbox-checked"></span></span><input type="checkbox" name="users[]" value="{{ entity.id }}" id="checkbox-table-2" data-toggle="checkbox"></label></td>

        {% for item in app_config.userTable %}
          {% if item.visible %}<td>{{ entity.get(item.fieldName) }}</td>{% endif %}
        {% endfor %}
{#     <td>
        <em>{{ entity.profile.firstName }} {{ entity.profile.familyName }}</em>
        {% if entity.enabled %}<span class="text-primary fui-radio-unchecked pull-right"></span>{% endif %}
        <div class="myprofile hide"> 
        <div class="row">
          <div class="col-md-4"><img class="myavatar img-responsive" src="{{ asset( entity.profile.image.getwebpath)}}"></div>
          <ul class="list-unstyled col-md-8 userinfo" data-index="{{ loop.index }}" data-id="{{ entity.id }}">
              <li><strong>Identifiant :</strong> {{ entity.username }}</li>
              <li><strong>Nom :</strong> {{ entity.profile.familyName }}</li>
              <li><strong>Preom :</strong>  {{ entity.profile.firstName }}</li>
              <li><strong>Date de naissance :</strong>  {{ entity.profile.birdDay|date('Y-m-d') }}</li>
              <li><strong>Sexe :</strong>  {{ entity.profile.gender }}</li>
              <li><strong>Adresse :</strong>  {{ entity.profile.address }}</li>
              <li><strong>Ville :</strong> {{ entity.profile.city }}</li>
              <li><strong>Siteweb :</strong> {{ entity.profile.website }}</li>
              <li><strong>Biographie :</strong> {{ entity.profile.description }}</li>
            </ul>
        </div>
        </div>
    </td>
    <td><a href="mailto:{{ entity.email }}">{{ entity.email }}</a></td>
    <td>{{ entity.status }}</td>
    <td>{{ entity.role }}</td> #}
        <td>
        <div class="myprofile hide"> 
        <div class="row">
          <div class="col-md-4"><img class="myavatar img-responsive" src="{{ asset( entity.profile.image.getwebpath)}}"></div>
          <ul class="list-unstyled col-md-8 userinfo" data-index="{{ loop.index }}" data-id="{{ entity.id }}">
              <li><strong>Identifiant :</strong> {{ entity.username }}</li>
              <li><strong>Nom :</strong> {{ entity.profile.familyName }}</li>
              <li><strong>Preom :</strong>  {{ entity.profile.firstName }}</li>
              <li><strong>Date de naissance :</strong>  {{ entity.profile.birdDay|date('Y-m-d') }}</li>
              <li><strong>Sexe :</strong>  {{ entity.profile.gender }}</li>
              <li><strong>Adresse :</strong>  {{ entity.profile.address }}</li>
              <li><strong>Ville :</strong> {{ entity.profile.city }}</li>
              <li><strong>Siteweb :</strong> {{ entity.profile.website }}</li>
              <li><strong>Biographie :</strong> {{ entity.profile.description }}</li>
            </ul>
        </div>
        </div>
        <div class="text-center">
          
        <div class="btn-group">
            <a class="btn btn-embossed btn-primary showuser btn-xs" data-index="{{ loop.index }}" data-id="{{ entity.id }}" href="#" data-toggle="modal" data-target="#myModal" title="afficher"><span class="glyphicon glyphicon-search"></span></a>
            <a class="btn btn-embossed btn-info btn-xs" href="{{ path('ben_edit_user', {id: entity.id }) }}" title="modifier"><span class="glyphicon glyphicon-edit"></span></a>
            <a class="btn btn-embossed btn-inverse btn-xs" href="{{ path('avancement_new', { 'id': entity.id }) }}" title="Ajouter un status"><span class="glyphicon glyphicon-user"></span></a>
            <a class="btn btn-embossed btn-default btn-xs" href="{{ path('cotisation_new', { 'id': entity.id }) }}" title="cotisation"><span class="glyphicon glyphicon-check"></span></a>
        </div>
        </div>
        </td>
    </tr>
{% endfor %}
                
{% include "BenAssociationBundle:Default:pagination.html.twig" %}

<script>   
(function($) {

    /* tooltip */
    $('td a').tooltip();

})(jQuery);
</script>
